server:
  port: 9002
spring:
  application:
    name: tensquare-base
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/tensquare_base?useSSL=false&serverTimezone=GMT&characterEncoding=utf-8
    username: root
    password: root
  jpa:
    database: MySQL
    show-sql: true
    generate-ddl: true
eureka:
  client:
    service-url:
      defaultZone: http://192.168.1.103:6868/eureka
